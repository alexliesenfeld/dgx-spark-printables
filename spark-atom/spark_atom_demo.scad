/*
 * Demo view: mount with the 150 × 150 × 50.5 mm device installed.
 * Choose a demo device underside with part_override below.
 */

part_override = "demo-gx10"; // [demo-gx10,demo-nvidia,demo-gigabyte,demo-msi,demo-acer,demo-lenovo]
include <spark_atom_mount.scad>
