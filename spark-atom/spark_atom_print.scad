/*
 * Print view: both side mounts plus the loose ventilated bottom plate.
 *
 * Both wall assemblies lie on their broad outside faces on the build plate.
 * Together with the loose plate, the compact layout fits a 250 x 250 mm bed.
 */

part_override = "print_layout";
include <spark_atom_mount.scad>
